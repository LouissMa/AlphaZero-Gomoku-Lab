name: PyTorch backend

on:
  push:
    paths:
      - "alphazero_gomoku/policy_value_net_pytorch.py"
      - "tests/test_policy_value_net_pytorch.py"
      - "pyproject.toml"
      - ".github/workflows/pytorch.yml"
  pull_request:
    paths:
      - "alphazero_gomoku/policy_value_net_pytorch.py"
      - "tests/test_policy_value_net_pytorch.py"
      - "pyproject.toml"
      - ".github/workflows/pytorch.yml"

permissions:
  contents: read

jobs:
  torch-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install training dependencies
        run: python -m pip install --upgrade pip ".[dev,train]"

      - name: Check modern backend
        run: ruff check alphazero_gomoku/policy_value_net_pytorch.py tests/test_policy_value_net_pytorch.py

      - name: Test modern backend
        run: python -m pytest tests/test_policy_value_net_pytorch.py
